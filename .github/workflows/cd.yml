name: CD (TestFlight Deploy)

on:
  push:
    branches:
      - main
      - develop
      - 'hotfix/*'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.9.1
        with:
          access_token: ${{ secrets.KEY }}

      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - uses: ruby/setup-ruby@v1

      - name: Install Bundler
        run: gem install bundler

      - name: Install Gems
        run: bundle install

      - name: Deploy to TestFlight (TEST)
        if: github.ref == 'refs/heads/develop'
        run: bundle exec fastlane test_flight_test

      - name: Deploy to TestFlight (PROD)
        if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/heads/hotfix/')
        run: bundle exec fastlane test_flight_prod
